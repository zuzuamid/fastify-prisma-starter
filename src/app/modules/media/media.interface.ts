export interface IMedia {
  name: string;
  url: string;
  folder?: string;
  type: string;
  size: number;
  uploadedBy?: number | null;
  altText?: string;
  title?: string;
  caption?: string;
  description?: string;
}

// Commit 59

// Commit 166
